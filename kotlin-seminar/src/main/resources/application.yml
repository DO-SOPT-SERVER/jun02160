spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sopt33?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: manbob73**

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: none
      format_sql: true

logging:
  level:
    org:
      springframework:
        security: TRACE